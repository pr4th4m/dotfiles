return {
  "Exafunction/codeium.nvim",
  branch = "main",
  event = { "BufReadPre", "BufNewFile" },
  config = function()
    require("codeium").setup({
      enable_chat = true,
      enable_cmp_source = true,
    })
  end
}
